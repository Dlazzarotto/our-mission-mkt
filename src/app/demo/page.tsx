import { CrmDashboard } from "@/components/crm-dashboard";

// Painel demonstrativo original (dados fictícios), preservado para referência visual.
export default function DemoPage() {
  return <CrmDashboard />;
}
